import Hero from '../../components/Hero';

const Portfolio = () => {
    return (
        <main className="overflow-hidden ">
            <Hero />
        </main>
    );
};

export default Portfolio;
